import { Url } from "url";
import { UrlResolver } from "@angular/compiler";

export interface IPlayer {
    playerName: string;
    playerNo: number;
    playerPos: string;
    playerVal: number;
    playerRating: number;
    contractDate: string;
   // imageUrl: ;

}
 